source "https://rubygems.org"

# Matches the Jekyll + plugin versions GitHub Pages runs server-side, so a
# local `bundle exec jekyll serve` behaves the same as the deployed site.
gem "github-pages", group: :jekyll_plugins

# Ruby 3 dropped webrick from the standard library; `jekyll serve` needs it.
gem "webrick", "~> 1.8"
